import java.util.Scanner;

     public class RANDOM
     {
        public static void main(String[] args) {
        {
            Scanner sc = new Scanner (System.in);
            
            //Question 5 - "RANDOM" guessing game using iteration
            
            char[] myList = new char [list.length];
            
            System.out.println ("Guess the six letter word using trial and error");
            String RANDOM = sc.nextLine();
            
            // Array list
            char[] result = new char[list.length];
            
            letters.add("R");
            letters.add("A");
            letters.add("N");
            letters.add("D");
            letters.add("O");
            letters.add("M");
            
            for (char i = letters.add){
                RANDOM.replaceAll("?",replacement);
                result[j] = list[i];
            }
            return result;
            
            System.out.println ("Your answer so far: " + RANDOM.toUpperCase());
            
            // CONCLUSION: I'm not sure how to use iteration or how to successfully create an array
            // in java 
            }

        }
    }
 

            